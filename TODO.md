# TODO — nuvem_de_som

## Open issues

None open.

## Gaps

- [ ] Duplicate lowercase `readme.md` tracked alongside `README.md` — remove the stale one.
- [ ] `pyproject.toml` Homepage URL points to `OpenJarbas/nuvem_de_som`; update to `TigreGotico/nuvem_de_som`.
- [ ] No type checker (mypy/pyright) in CI despite fully annotated source — lint runs ruff only.
- [ ] Whole library lives in a single ~1.4k-line `__init__.py`; consider splitting backends/converters into modules.

## Code TODOs

None found. (FIXME hits under `test/cassettes/` are inside recorded upstream JS, not project source.)
